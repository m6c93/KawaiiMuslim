import test from 'node:test';
import assert from 'node:assert/strict';
import {PLOTS,treeStage,treeAppearance,treeHealth,cleanGarden,placeTree,removeTree} from '../applications/coran/garden.mjs';
test('the tree grows through five stages based on surah completion',()=>{assert.equal(treeStage(1).file,'seedling');assert.equal(treeStage(25).file,'sapling');assert.equal(treeStage(50).file,'young');assert.equal(treeStage(75).file,'blossom');assert.equal(treeStage(100).file,'apple')});
test('all 114 surahs have a distinct stable visual variant',()=>{assert.equal(new Set(Array.from({length:114},(_,i)=>JSON.stringify(treeAppearance(i+1,50)))).size,114)});
test('freshness changes after seven and eight days without losing trees',()=>{const last=100;assert.equal(treeHealth(last,last+6*86400000).thirsty,false);assert.equal(treeHealth(last,last+7*86400000).thirsty,true);assert.equal(treeHealth(last,last+8*86400000).faded,true);assert.equal(treeHealth(last+8*86400000,last+8*86400000).faded,false)});
test('one earned surah tree can be placed in one of twelve spots',()=>{const completed={'1:1':true,'1:2':true,'2:1':true},state=cleanGarden(null,completed);assert.equal(PLOTS,12);assert.equal(placeTree(state,'1:1',4,completed),true);assert.equal(placeTree(state,'1:2',6,completed),true);assert.deepEqual(state.positions,{'1':6});assert.equal(placeTree(state,'2',6,completed),false);assert.equal(placeTree(state,'3',8,completed),false);removeTree(state,'1:2');assert.deepEqual(state.positions,{})});
test('old verse trees migrate to one tree per surah and invalid spots disappear',()=>{const c={'1:1':true,'1:2':true,'2:1':true};const s=cleanGarden({positions:{'1:1':1,'1:2':2,'2:1':9,'9:9':4},watered:{'1:1':123,'1:2':456,'9:9':23}},c);assert.deepEqual(s,{positions:{'1':1,'2':9},watered:{'1':456}})});

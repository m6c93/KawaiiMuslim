const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,channel:'chrome'});try{const p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));const origin=process.env.TEST_ORIGIN||'http://127.0.0.1:4319',school=origin+'/presentation-coran/?local=1&view=school';await p.goto(school);await p.getByRole('button',{name:'Présences',exact:true}).click();await p.getByText('Appel non renseigné',{exact:true}).first().waitFor();await p.getByRole('button',{name:'Professeurs',exact:true}).click();await p.getByRole('link',{name:'Ouvrir son espace professeur'}).first().click();await p.getByRole('button',{name:'Ouvrir la classe',exact:true}).first().click();const url=p.url();await p.getByRole('button',{name:/Faire l’appel/}).click();let d=p.getByRole('dialog');await d.getByRole('button',{name:'Enregistrer l’appel'}).click();await d.getByText(/Choisissez une présence/).waitFor();await d.getByRole('button',{name:'Tous présents'}).click();const pupil=d.locator('fieldset').first();await pupil.getByLabel('Absent',{exact:true}).check();await pupil.getByLabel('Absence justifiée').check();await pupil.getByLabel('Petite note').fill('Prévenu par le parent');await d.getByRole('button',{name:'Enregistrer l’appel'}).click();await d.getByText(/Appel enregistré/).waitFor();await d.getByRole('button',{name:'Fermer',exact:true}).click();await p.reload();await p.getByRole('button',{name:'Ouvrir la classe',exact:true}).first().click();await p.getByRole('button',{name:/Faire l’appel/}).click();d=p.getByRole('dialog');assert.ok(await d.locator('fieldset').first().getByLabel('Absent',{exact:true}).isChecked());await p.setViewportSize({width:390,height:844});assert.ok(await d.evaluate(e=>e.scrollWidth<=e.clientWidth+1));await p.goto(school);await p.getByRole('button',{name:'Présences',exact:true}).click();await p.getByText(/1 absent\(s\)/).waitFor();await p.getByText(/Prévenu par le parent/).waitFor();assert.deepEqual(errors,[]);console.log('PASS: optional call, explicit statuses, justified absence/note, reload persistence, director summary, mobile.')}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});

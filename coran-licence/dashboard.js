const ACCOUNT_KEY='km-coran-pro-account-v1';let account=JSON.parse(localStorage.getItem(ACCOUNT_KEY)||'null');if(!account){location.replace('inscription.html');throw new Error('Compte pilote requis')}
const save=()=>localStorage.setItem(ACCOUNT_KEY,JSON.stringify(account));const esc=value=>String(value).replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[char]));const createPanel=document.querySelector('#createPanel');const classesView=document.querySelector('#classesView');const toast=document.querySelector('#toast');
document.querySelector('#accountName').textContent=`${account.firstName} ${account.lastName}`;document.querySelector('#accountInitial').textContent=(account.firstName[0]||'P').toUpperCase();document.querySelector('#accountPlan').textContent=`Licence ${account.plan}`;document.querySelector('#sidePlan').textContent=account.plan;document.querySelector('#organizationHeader').textContent=account.organization;document.querySelector('#welcomeTitle').textContent=`Bienvenue ${account.firstName} 👋`;
function render(){const classes=account.classes||[];document.querySelector('#classCount').textContent=classes.length;document.querySelector('#studentCount').textContent=classes.reduce((sum,item)=>sum+(item.students||0),0);document.querySelector('#onboarding').hidden=classes.length>0;if(!classes.length){classesView.className='';classesView.innerHTML='<div class="empty-class"><span>🌱</span><h3>Aucune classe pour le moment</h3><p>Votre première classe accueillera les comptes élèves, les récitations et leurs jardins.</p><button class="button" id="emptyCreate" type="button">Créer ma première classe</button></div>';document.querySelector('#emptyCreate').addEventListener('click',openPanel);return}classesView.className='class-grid';classesView.innerHTML=classes.map(item=>`<article class="class-card"><div class="class-card-top"><span class="class-card-icon">🌿</span><button class="button ghost" type="button">Ouvrir</button></div><h3>${esc(item.name)}</h3><p>${esc(item.level)}</p><div class="class-meta"><span>${item.students||0} élève</span><span>0 récitation</span><span>Jardin prêt</span></div></article>`).join('')}
function openPanel(){createPanel.classList.add('open');createPanel.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>createPanel.querySelector('input').focus({preventScroll:true}),400)}
['#newClassTop','#newClassSecondary','#startClass'].forEach(selector=>document.querySelector(selector)?.addEventListener('click',openPanel));document.querySelector('#classForm').addEventListener('submit',event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.currentTarget));account.classes=[...(account.classes||[]),{id:crypto.randomUUID?.()||String(Date.now()),name:data.className.trim(),level:data.level,students:0,createdAt:new Date().toISOString()}];save();event.currentTarget.reset();createPanel.classList.remove('open');render();toast.textContent=`La classe « ${data.className.trim()} » est prête 🌱`;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3500)});render();
